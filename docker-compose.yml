version: '3.8'

services:
  warrior-dashboard:
    build: .
    container_name: warrior-dashboard
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
